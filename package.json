{
  "name": "photo_report_app",
  "version": "1.0.0",
  "description": "A photo report application for roofing inspections with AI analysis",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start"
  },
  "keywords": [
    "photo",
    "report",
    "roofing",
    "ai",
    "analysis"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "gridfs-stream": "^1.1.1",
    "mongoose": "^8.12.1",
    "multer": "^1.4.5-lts.1"
  }
}
